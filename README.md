# Annealed-interactions

## Aim

This package analyses the moments of the distribution of interactions coefficients of a model showing gLV dynamics.

## Required packages

To install and load the packages used in this repository, you can run the following R script, just run:

``` r
source("R/packages.R")
```
